import { Environment } from '../app/core/models/environment';

export const environment: Environment = {
  production: true,
  appName: 'STOCKBFLOW-WEB',
  envName: 'PROD',
  test: false,
  baseUrl: '',
};
